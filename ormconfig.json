{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "root",
  "password": "secure",
  "database": "ihb",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "subscribers": ["dist/**/*.typeorm-subscriber{.ts,.js}"],
  "synchronize": true,
  "logging": true,
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
